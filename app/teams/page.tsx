import * as React from 'react';

export type Props = {
}

function TeamPage(props: Props) {
  const {
  } = props;

  return (
    <div>
      page
    </div>
  );
}

export default React.memo(TeamPage)
